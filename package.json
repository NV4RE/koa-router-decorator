{
  "name": "@nv4re/koa-decorator",
  "description": "@route decorator for koa-router (fork from https://github.com/gwuhaolin/koa-router-decorator)",
  "license": "MIT",
  "repository": "https://github.com/NV4RE/koa-router-decorator",
  "keywords": [
    "koa",
    "router",
    "decorator",
    "@route"
  ],
  "version": "2.0.0",
  "scripts": {
    "build": "npm run clean && tsc --outDir build",
    "clean": "rimraf build"
  },
  "publishConfig": {
    "access": "public"
  },
  "typings": "index",
  "devDependencies": {
    "@types/node": "^12.0.10",
    "rimraf": "^2.6.3",
    "typescript": "^3.5.2"
  },
  "dependencies": {
    "@types/koa-router": "^7.0.42"
  }
}
